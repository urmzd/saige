// Package types defines the sealed core types shared across the agent
// subsystem: Message, Content, Delta, Tool and ToolRegistry, Provider,
// Embedder, Cache, Compactor, StepRunner, and node/marker types for the
// conversation tree and human-in-the-loop gating.
package types
